language: python
sudo: false
python:
  - "3.4"
  - "3.5"
env:
  - DJANGO="Django>=1.8" DB=sqlite
install:
  - pip install -U $DJANGO --quiet
  - pip install -r requirements.pip

notifications:
  email:
    on_success: never
    on_failure: always